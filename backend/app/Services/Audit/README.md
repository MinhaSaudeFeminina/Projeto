# Auditoria

Auditoria append-only para eventos editoriais, permissões e solicitações LGPD.

Eventos devem registrar ator, ação, alvo, estado anterior, estado novo e data/hora, evitando dados íntimos desnecessários em metadados.
